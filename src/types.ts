export type CategoryCardProps = {
    id: number;
    name: string;
    icon: string;
    quantity: number;
};

export type ProductProps = {
    id: number;
    name: string;
    price: string;
    category: string;
    image: string;
    description: string;
};
